(no external research applicable)

The `.agents/` partition (partition 2) is overwhelmingly static markdown skill content — 43 SKILL.md files with YAML frontmatter (`name`, `description`, optional `metadata.provider`) plus reference docs and a handful of Python helper scripts. No third-party library imports drive functionality here. The SKILL.md frontmatter format is already fully documented in the local project at `docs/claude-code/cli/skills.md`, which was fetched from the Anthropic Claude Code docs and covers the complete skills spec (discovery, loading, `disable-model-invocation`, context costs, and the `.claude/skills` symlink convention). The rewrite onto pi-coding-agent treats SKILL.md content as agent-agnostic; the only Atomic/Claude-specific coupling is the loading mechanism (`.claude/skills` symlink pointing to `.agents/skills`, and `~/.agents/skills` as the install directory) — both of which are already captured in the local docs and CLAUDE.md project instructions. Fetching the Anthropic Claude Code skills documentation externally would be redundant given the authoritative local copy, and no other external library or framework is central to answering how this partition behaves in the pi-coding-agent rewrite.
